<div class="{{generalBodyCLass}}">
  <h2 class="pageTitle"> {{pageTitle}} </h2>

    <div class="userPlaylists">
      {{#each playlist}}
        <h3> {{this.title}} </h3>
      {{/each}}
    </div>
  </div>

</div>
